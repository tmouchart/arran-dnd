services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: arrandnd
      POSTGRES_USER: arran
      POSTGRES_PASSWORD: arran_dev
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data

volumes:
  pg_data:
