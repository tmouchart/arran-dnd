# Rename `app` to match `fly apps create <name>` or run `fly launch` once to bind.
app = "arran-dnd"
primary_region = "cdg"

[build]

[deploy]
  release_command = "node server/dist/scripts/migrate.js"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0

[http_service.concurrency]
  type = "requests"
  soft_limit = 200
  hard_limit = 250

[[http_service.checks]]
  grace_period = "10s"
  interval = "30s"
  method = "GET"
  timeout = "5s"
  path = "/api/health"
